# 面接日程調整アプリ 設計ドキュメント

作成日: 2026-09-14
最終更新: 2026-09-16(設計レビューを受けて改訂)
作成者: 関 光洋

---

## 1. 何を解決するのか

社内の採用担当者(現状6名。全員が「採用担当 兼 面接官」を兼務している)は、候補者から届いた希望日時をもとに、社内メンバーのカレンダーを一人ずつ開いて空き状況を確認し、対応可能な人に個別に声をかけて面接を組んでいる。この作業は手間がかかるうえ、複数の担当者が同時に動くことで**ダブルブッキング(二重予約)**や**候補者への連絡漏れ**といった確認ミスが発生している。

本アプリでは、**候補者からもらった希望日時(3〜5件)をまとめて入力すると、それぞれの日時について対応できる担当者が一覧で分かり、そのまま予約(アサイン)まで完結できる仕組み**を提供する。これにより、カレンダーを人力で確認する手間と、確認ミスによるトラブルを減らすことを目指す。

---

## 2. やること / やらないこと

### やること(今回のスコープ)

- **候補者の希望日時(3〜5件)をまとめて入力すると、それぞれの日時ごとに対応できる担当者が一覧表示される**(候補日を1つずつカレンダーで確認する方式はやめ、まとめて比較できる形にした。詳細は3章)
- 担当者を1人以上選択してアサインし、その場で**即時確定**する(仮予約・承認フローは設けない)
- 確定した予約は**全担当者が閲覧できる**(誰が・いつ・どの面接を持っているかが分かる状態にし、ダブルブッキングを防ぐ)
- 予約の**キャンセル・日程変更**
- 担当者が各自ログインし、**自分の既存の予定(空いていない時間帯)を登録**する
- 稼働時間帯(10:00〜19:00、12:00〜13:00は不可)・面接時間60分固定(開始は15分刻み)という制約を踏まえた空き判定
- **候補者の氏名(メモ程度)をデータとして保存する**(予約一覧・詳細で「誰の面接か」が分かるようにするため)

### やらないこと(スコープ外)とその理由

| やらないこと | 理由 |
|---|---|
| 候補者の希望日時そのものを保存すること | 検索のたびに毎回入力する運用とし、「候補日一覧」を独立したデータとしては永続化しない(確定した面接だけがデータとして残る) |
| 候補者情報のうち、氏名以外の項目(応募職種・連絡先など) | 「誰の面接か分かればよい」という目的には氏名だけで十分なため。管理項目を増やすと入力の手間が増える |
| 候補者の重複統合(同姓同名の名寄せ、候補者検索) | 候補者は検索のたびに新規データとして作成する。同一人物の複数面接をまとめて管理する機能は今回作らない |
| 2次・3次面接の管理 | 現状1次面接のみ運用しているため。将来的な拡張の余地は残す設計にする |
| 候補者自身による希望日時の直接入力 | 候補者はシステム利用者ではなく、現状も担当者が代理で対応する運用のため |
| Googleカレンダー等の外部カレンダー連携 | 将来的な要望はあるが、今回はダミー・手動データで完結させる |
| 稼働時間帯・休憩時間を変更するための**管理画面(UI)** | 今回は初期値(10:00〜19:00、休憩12:00〜13:00)で運用するが、**値自体はコードに埋め込まず、DB上の設定データとして持たせる**(詳細は4章)。将来、値を変更したい・曜日ごとに変えたいといった要望が出たときに、設定用の画面を追加するだけで対応できるようにしておく |
| 候補者への確定連絡の自動送信(メール等) | 現状も手動運用であり、通知の自動化は将来の理想と位置づけられているため |
| ロール別の権限管理(画面・機能の出し分け) | 全員が「採用担当 兼 面接官」という同一ロールであるため不要 |

---

## 3. 置いた仮定

ヒヤリングで確認しきれなかった点、および設計レビューでの指摘を踏まえて以下のように仮定・変更を行った。

- **「候補日を1つずつカレンダーでクリックして確認する」というUXは、候補者から複数の候補日をまとめてもらう実際の業務フロー(3〜5件)に対して手数が多く使い勝手が悪い、というレビュー指摘を受け、「候補日時をまとめて入力すると、比較結果が一覧表示される」という方式に変更した。** 入力した候補日時そのものは保存せず、その場で空き状況を計算して表示するだけの扱いとする(確定した面接だけがデータとして残る)
- **候補者に関する情報を一切持たない設計だと、予約一覧を見たときに「誰の、何の面接か分からなくなる」というレビュー指摘を受け、氏名(メモ程度の自由入力)だけを持つ`candidates`テーブルを追加した。** 検索するたびに新しいレコードを作成し、同姓同名の名寄せや候補者の検索機能は今回作らない(将来、2次・3次面接を管理するようになった際に、同一候補者への複数面接を紐づける土台として使える)
- 担当者の「空き」判定は、既存の予定と面接時間が**少しでも重なっていれば「空いていない」**とみなす(部分的な重複を許容しない)
- 1回の面接に複数の担当者をアサインできる(パネル面接に対応する)。ただし必須人数(最低◯人)の指定は設けず、1人以上を選べればよいとする
- **稼働時間帯・休憩時間は、コードに直接書き込む固定値ではなく、DB上の設定データ(初期値: 10:00〜19:00、休憩12:00〜13:00)として持たせる。**今回はこの値を変更するUIは作らないが、将来「営業時間を変えたい」「曜日ごとに変えたい」となったときに、データ構造自体は作り直さずに済むようにしておく
- タイムゾーンは日本標準時のみを扱う
- ログイン機能は簡易なもの(メールアドレス+パスワードでの認証)とし、パスワードリセットや招待フローなどの周辺機能は作らない
- ダブルブッキング防止は「予約確定時に、対象の担当者が実際に空いているかをサーバー側で再チェックする」方式とし、それ以上の高度な排他制御(同時アクセス時のロック等)は今回のスコープでは扱わない

---

## 4. データの持ち方

### 主なテーブル

**users(担当者。採用担当 兼 面接官)**
| カラム | 型 | 説明 |
|---|---|---|
| id | integer | |
| name | string | 氏名 |
| email | string | ログインID |
| password_digest | string | 認証用 |

**availabilities(担当者の既存の予定 = 空いていない時間帯)**
| カラム | 型 | 説明 |
|---|---|---|
| id | integer | |
| user_id | integer | 誰の予定か |
| start_at | datetime | 予定の開始 |
| end_at | datetime | 予定の終了 |
| note | string | 予定の内容(任意) |

**candidates(候補者。氏名のみのメモ的な情報)**
| カラム | 型 | 説明 |
|---|---|---|
| id | integer | |
| name | string | 候補者名(自由入力のメモ、例:「山田さん(エンジニア職)」) |

**interviews(確定した面接予約)**
| カラム | 型 | 説明 |
|---|---|---|
| id | integer | |
| candidate_id | integer | どの候補者の面接か(candidates.id) |
| scheduled_start_at | datetime | 面接開始日時 |
| scheduled_end_at | datetime | 面接終了日時(開始+60分) |
| status | string | confirmed / cancelled |
| created_by_id | integer | 登録した担当者(users.id) |

**interview_assignments(interviewsとusersの中間テーブル)**
| カラム | 型 | 説明 |
|---|---|---|
| id | integer | |
| interview_id | integer | |
| user_id | integer | アサインされた担当者 |

1回の面接に複数の担当者(パネル面接)をアサインできるようにするため、`interviews`と`users`を直接1対多で結ばず、間に中間テーブルを挟んでいる(1つの面接に複数の担当者、1人の担当者が複数の面接を持てる「多対多」の関係を表現するため)。この関係が不要になる(必ず1面接=1担当者になる)という仕様変更がない限り、このテーブルは維持する。

**business_hours(稼働時間帯・休憩時間の設定)**
| カラム | 型 | 説明 |
|---|---|---|
| id | integer | |
| start_time | time | 稼働開始時刻(初期値 10:00) |
| end_time | time | 稼働終了時刻(初期値 19:00) |
| break_start_time | time | 休憩開始時刻(初期値 12:00) |
| break_end_time | time | 休憩終了時刻(初期値 13:00) |
| interview_duration_minutes | integer | 面接時間(初期値 60) |
| slot_interval_minutes | integer | 選択可能な開始時刻の刻み(初期値 15) |

現時点では常に1件だけ存在する設定テーブルとして扱う(将来、曜日別・拠点別などに拡張する場合はここにカラムを足す想定)。空き判定のロジックは、この設定値をコードから直接参照する形にし、`10:00`や`19:00`のような数値をコード中に直接書かない。

### 設計の考え方

「担当者自身の予定(availabilities)」と「確定済みの面接予約(interviews)」を別テーブルに分けることで、空き判定は次の3つのチェックを組み合わせて行う。

1. 指定した時間帯が、その担当者の `availabilities`(=既存の予定)と重ならないか
2. 指定した時間帯が、その担当者がアサインされている `interviews`(=すでに確定した面接)と重ならないか
3. 指定した時間帯が `business_hours` の稼働時間帯・休憩時間の範囲内か

候補者からもらった希望日時そのもの(検索用に入力する3〜5件)を保存するテーブルは持たない。画面上で入力・比較した結果、実際に確定したものだけが`interviews`としてデータに残る。

---

## 5. 画面と操作の流れ

### 画面一覧

| No | 画面 | 誰が | 何をする |
|---|---|---|---|
| 1 | ログイン画面 | 全担当者 | メールアドレス・パスワードでログイン |
| 2 | 日程検索・比較画面 ★メイン機能 | 担当者 | 候補者名と希望日時(3〜5件)をまとめて入力すると、それぞれの日時ごとに対応できる担当者が一覧表示される。担当者を1人以上選んでその場で確定する |
| 3 | 予約一覧画面 | 全担当者 | 確定済み・キャンセル済みの予約を一覧で確認(誰の、いつの、どの面接を持っているか) |
| 4 | 予約詳細画面 | 担当者 | 個別の予約の詳細確認、キャンセル・日程変更 |
| 5 | 自分の予定 登録画面 | 担当者本人 | 自分の既存の予定(空いていない時間帯)を登録・編集 |

### 操作の流れ(業務フロー)

```
候補者から日程候補(3〜5件)をもらう(メール等、システム外)
        ↓
② 日程検索・比較画面 で、候補者名と候補日時をまとめて入力
        ↓
   候補日時ごとに、対応できる担当者が一覧表示される
        ↓
   日時と担当者(1人以上)を選択して「確定」を押す → 即時確定(仮予約なし)
        ↓
③ 予約一覧画面 に反映され、全担当者が確認できる
        ↓
候補者への確定連絡はシステム外で手動対応
```

なお、確定の直前に他の担当者が同じ枠を先に確定させていた場合(ダブルブッキングになりかけた場合)は、その場でエラーを表示し、**同じ入力内容のまま**最新の空き状況を再計算した②の画面に戻す(候補者名・候補日時を入力し直す必要はない)。

---

## 6. 作る順番

Rails未経験であることを踏まえ、**小さく確実に動くものを積み上げる順番**にする。

1. **User(担当者)のログイン機能 + ダミーの予定データ(availabilities)の投入**
   → まず「データがある状態」を作り、Railsの基本構成(モデル・DB)に慣れる
2. **自分の予定 登録画面(⑤)**
   → 単純なCRUD画面。Railsのモデル・コントローラ・ビューの基本を習得しながら作る
3. **日程検索・比較画面(②)— 候補日時ごとに対応できる担当者を表示するロジック**
   → この課題で最も重要かつ不確実性の高い機能。`business_hours`の設定値を使った空き判定ロジックをここで検証する
4. **予約確定処理(interview + candidate作成、ダブルブッキング防止のチェック)**
   → ③の続きとして、確定時にサーバー側で空き状況を再チェックする処理を組み込む
5. **予約一覧・詳細画面(③④)とキャンセル・変更機能**

**この順番にした理由**: 最も不確実性が高く、この課題の核心である「候補日時の入力→空き判定→比較表示」の一連の流れを早い段階で動かして検証したいため。逆に自分の予定登録画面のような単純なCRUD画面は、Railsの基礎を掴む練習も兼ねて先に着手する。
