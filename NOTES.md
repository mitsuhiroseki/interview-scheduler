# 判断メモ

## AIにどんなことを聞いたか(代表的なもの)

- Day2ヒヤリング前の質問リストの壁打ち(業務フロー、空き判定の基準、面接官の条件など、聞くべき論点の洗い出しと優先順位づけ)
- ヒヤリング結果をもとにした設計ドキュメント(DESIGN.md)のドラフト作成
- Railsの基本的な設計概念の解説(例:「中間テーブル(interview_assignments)」がなぜ必要か、`user_id`カラムを1つ追加するだけでは多対多の関係を表現できない理由)
- ログイン機能の実装内容(`has_secure_password`によるパスワードのハッシュ化、セッションベースの認証)の仕組みの解説
- 設計レビューで受けたフィードバック(UXの使い勝手、候補者情報の要否、テーブル構成の見直し)をどう設計・実装に反映するかの相談

## AIの提案のうち、採用しなかったもの

- **カレンダーの月表示に、空き人数を色分け表示するヒートマップ案**:レビュー指摘への対応として提案されたが、「候補日時をまとめて入力し、一覧で比較する」方式だけで使い勝手の問題は解決できると判断し、実装コストの高いヒートマップ表示は見送った
- **候補者情報に応募ポジションなどの項目も持たせる案**:「誰の面接か分かればよい」という当初の目的には氏名だけで十分と判断し、管理項目は最小限(氏名のみ)にした
- **軽量CSSフレームワーク(Bootstrap等)の導入案**:見た目は整うが、外部依存が増え、Rails未経験の自分がコードの中身を理解しきれなくなる懸念があったため、シンプルな独自CSSで見やすさだけ改善する方針を選んだ
- **稼働時間帯・休憇時間を管理画面から変更できるようにする案**:将来的な拡張の余地は残したい(DB上の設定値として持たせる)が、今回のスコープでは時間的な制約もあり、設定用の画面までは作らないと判断した

## 実装中に気づいて、設計から変えた点

- 当初は「候補者情報は一切管理しない」設計だったが、実装後のレビューで「予約一覧を見たときに誰の面接か分からなくなる」という指摘を受け、氏名だけを持つ`candidates`テーブルを追加した
- 当初は「カレンダー上で任意の日付をクリックして、その日の空き状況を確認する」というUXだったが、実際の業務では候補者から3〜5件の候補日をまとめてもらっている(ヒヤリングで確認済み)ため、1日ずつクリックして確認する方式は実態に合わず使い勝手が悪いという指摘を受けた。「候補日時をまとめて入力すると、一覧で比較できる」方式に変更した
- 上記のUX変更に伴い、ダブルブッキングを検知して確定に失敗した場合の戻り先も変更した。当初はカレンダーの特定の日付ページに戻す設計だったが、変更後は入力した検索条件(候補者名・候補日時一覧)を保持したまま検索画面に戻し、最新の空き状況をその場で再確認できるようにした
- 実装中、`json` gemのバージョン(3.x系)がRails 8.1 + Ruby 3.4の組み合わせでセッションCookieの復号エラーを起こす問題に遭遇した。設計とは直接関係ないが、Gemfileでバージョンを`~> 2.9`に固定して解決した

## 時間があれば次にやりたいこと

- 候補者からのメール文面などから、希望日時を自動で読み取る機能(自然言語からの日時抽出)
- Googleカレンダーとの連携(担当者の予定を自動同期)
- 稼働時間帯・休憩時間を管理画面から変更できるようにする(現状はDBを直接更新すれば変更可能だが、UIは未実装)
- 2次・3次面接を含めた選考プロセス全体の管理(候補者ごとに複数の面接を紐づける)
- 自動テスト(RSpec)の追加。今回は手動確認(curlおよびブラウザ)のみで動作検証した
